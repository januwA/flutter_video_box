name: video_box
description: A control that plays video in flutter, I make the control as flexible as possible, can play a single video, video list on the page.
version: 0.17.0
homepage: https://github.com/januwA/flutter_video_box

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.2.7
  connectivity_plus: ^2.0.2 # 监听网络连接
  sensors_plus: ^1.2.1 # 监听设备旋转

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
